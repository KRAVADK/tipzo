// Deployed program: donatu_appv4.aleo (build/main.aleo)
// Compatible with: profiles mapping, create_profile, update_profile, send_donation
// Transitions: create_profile(name, bio), update_profile(name, bio), send_donation(sender, recipient, amount, message, timestamp)
// Mappings: profiles (address -> ProfileInfo), donation_index, sent_donation_index

export const PROGRAM_ID = "donatu_appv4.aleo";
